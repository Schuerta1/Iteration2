<!DOCTYPE html>
<html>
  <head>
    <title>Iteration2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <div style="width: 20%; float:left; text-align: left">
      <nav class="navbar navbar-light">
        <a class="navbar-brand" href="http://localhost:3000/">HOME</a>
      </nav>
    </div>
    <div style="float: right; padding-right: 15px; padding-top: 5px;">
      <p class="navbar-text float-right">
        <% if user_signed_in? %>
          Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to 'Edit profile', edit_user_registration_path, :class => 'btn btn-outline-info' %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-secondary', style:"color:white"  %>
        <% else %>
          <%= link_to "Login", new_user_session_path, :class => 'btn btn-outline-primary', style:"color:blue"  %>
          <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-primary', style:"color:white"  %> 
        <% end %>
      </p>
    </div>
    <br>
    <br>
    <%= yield %>
    <p class="notice"><%= notice %></p> 
    <p class="alert"><%= alert %></p> 
  </body>
</html>
